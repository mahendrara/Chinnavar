/* 
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
//var wsUri = "ws://" + document.location.host + "/openSchedule-war/wsendpoint";
//if(typeof websocket === undefined) 
 //   var websocket = typeof(websocket) === "undefined"? new WebSocket(wsUri) : websocket;

/*websocket.onopen = function(evt) {
    onOpen(evt);
};
websocket.onerror = function(evt) {
    onError(evt);
};
websocket.onmessage = function(evt) {
    onMessage(evt);
};

websocket.onclose = function() {
   //alert("close");
};

var msgTypes = ["SET_FILTERS", "CHANGE_SETTINGS", "KPI_INSTANT_DATA", "KPI_HISTORY_DATA"];

window.onbeforeunload = function() {
    websocket.close();
};

function sendText(json) {
    console.log("sending text: " + json);
    websocket.send(json);
}

// For testing purposes
var output = document.getElementById("test");
function writeToScreen(message) {
    output.innerHTML += message + "<br>";
}

function onOpen(evt) {
    
}
function onError(evt) {
    writeToScreen('<span style="color: red;">ERROR:</span> ' + evt.data);
}*/





